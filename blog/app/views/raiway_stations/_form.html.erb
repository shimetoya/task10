<%= form_for(@raiway_station) do |f| %>
  <% if @raiway_station.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@raiway_station.errors.count, "error") %> prohibited this raiway_station from being saved:</h2>

      <ul>
      <% @raiway_station.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
